<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="R3Ext.SampleApp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Label
                x:Name="TickerLabel"
                Text="Ticker: -"
                SemanticProperties.HeadingLevel="None" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button
                    Text="Start Ticker"
                    Clicked="OnStartTicker"
                    Grid.Column="0" />
                <Button
                    Text="Stop Ticker"
                    Clicked="OnStopTicker"
                    Grid.Column="1" />
            </Grid>

            <Label x:Name="NameLabel" Text="Name: (unset)" />
            <Entry x:Name="NameEntry" Placeholder="Edit name" />
            <Label x:Name="UpperNameLabel" Text="Upper: (unset)" />
            <Label x:Name="StatusLabel" Text="Status: -" />
            <Label x:Name="DeepNameLabel" Text="Deep: (unset)" />
            <Label x:Name="MixedNameLabel" Text="Mixed: (unset)" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
